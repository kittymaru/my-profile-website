# Portfolio Website
A responsive single-page application (SPA) profile website built with React and React Router and styled with TailwindCSS, designed to showcase personal information, skills, and projects.

## Getting Started
After cloning the repository, run `npm install`. Then, run locally with:
`npm run dev`.

Open your browser and navigate to:
`http://localhost:5173`
